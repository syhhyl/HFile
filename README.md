# HFile

HFile is a lightweight terminal-based transfer tool written in C for files and messages.

It is designed around a simple idea: make peer-to-peer transfer clear, controllable, and easy to extend.

## Features

- File and message transfer over TCP
- Custom binary protocol

## how to build

See `./build.sh -h` for build usage.
